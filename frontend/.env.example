# GitHub API — used by the Projects page to fetch your repos
# Get a token at: https://github.com/settings/tokens
# Needs no scopes (public repos only) — just increases rate limit from 60 to 5000 req/hr
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_USERNAME=username
